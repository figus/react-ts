export default interface IProduct {
  barcode: string,
  hasCupString: boolean,
  cupString: string,
  image: string,
  price: number,
  name: string,
  packageSize: string,
  origin: string,
  onSale: boolean,
}